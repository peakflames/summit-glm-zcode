{
  "name": "summit",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "description": "Summit — a single-page habit tracker (public reference example for the peak-workflow plugin)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^26.4.1",
    "eslint": "^9.9.0",
    "happy-dom": "^20.14.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.5.0",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
